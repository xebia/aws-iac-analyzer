[settings]
model_id = anthropic.claude-3-5-sonnet-20241022-v2:0
public_load_balancer = False

# Authentication Settings
authentication = False
auth_type = none  # Possible values: none, new-cognito, existing-cognito, oidc

# HTTPS/SSL Certificate Settings (Required if authentication=True)
certificate_arn = 

# New Cognito Settings (Required if auth_type=new-cognito)
cognito_domain_prefix = 
callback_urls = https://your-alb-domain/oauth2/idpresponse
logout_url = https://your-alb-domain  # Single logout URL for new-cognito

# Existing Cognito Settings (Required if auth_type=existing-cognito)
existing_user_pool_arn = 
existing_user_pool_client_id = 
existing_user_pool_domain = 
existing_cognito_logout_url = # Logout URL for existing Cognito

# OIDC Settings (Required if auth_type=oidc)
oidc_issuer = 
oidc_client_id = 
oidc_client_secret = 
oidc_authorization_endpoint = 
oidc_token_endpoint = 
oidc_user_info_endpoint = 
oidc_logout_url = # Logout URL for OIDC